# Analyst (optional — every measurement endpoint works without it)
ANTHROPIC_API_KEY=

# Billing (optional — free tier is fully functional without it)
STRIPE_SECRET_KEY=
STRIPE_PRICE_DESK=
STRIPE_PRICE_FIRM=
STRIPE_PRICE_ENTERPRISE=
STRIPE_WEBHOOK_SECRET=
PUBLIC_ORIGIN=

# Static partner keys, comma-separated "key:tier"
ASSAY_KEYS=
